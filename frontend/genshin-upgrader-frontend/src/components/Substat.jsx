/* eslint-disable no-unused-vars */
import React from 'react';
// import '../styles/Substat.scss';

export default function Substat({ children }) {
  return (
    <div
      className="Substat"
    >
      {children}
    </div>
  );
}
