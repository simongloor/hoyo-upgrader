/* eslint-disable no-unused-vars */
import React from 'react';
// import '../styles/CharacterStatGroup.scss';

export default function CharacterStatGroup({ children }) {
  return (
    <div
      className="CharacterStatGroup"
    >
      {children}
    </div>
  );
}
