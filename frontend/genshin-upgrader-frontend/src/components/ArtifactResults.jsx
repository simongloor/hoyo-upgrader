/* eslint-disable no-unused-vars */
import React from 'react';
// import '../styles/ArtifactResults.scss';

export default function ArtifactResults({ children }) {
  return (
    <div
      className="ArtifactResults"
    >
      {children}
    </div>
  );
}
